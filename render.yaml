services:
  - type: web
    name: crewai-backend
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: HUGGINGFACE_API_TOKEN
        fromSecret: HUGGINGFACE_API_TOKEN
